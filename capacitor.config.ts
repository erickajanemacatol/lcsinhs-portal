import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lcsinhs.front',
  appName: 'lcsinhs-front',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
